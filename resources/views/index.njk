{% extends 'master' %}

{% block content %}

    <div id="hypegame">
        <div class="release-column">

        {% for release in releases %}

        <release name="{{ release.name }}" description="{{ release.description }}" image="{{ release.image }}" hype="{{ release.hype }}" hypable="true">
        </release>

        {% endfor %}

        </div>
    </div>

    <div class="copyright">
        <p>Copyright <a href="http://johnsparwasser.com">John Sparwasser</a> <script>document.write(new Date().getFullYear())</script></p>
    </div>

{% endblock %}

